# EconDaily · 变更日志

> 经管论文推送小助手 · 单文件 HTML，无后端，数据来自 OpenAlex 公共 API。
> 线上地址：https://annierosin.github.io/econdaily/

---

## 2026-09-25 · 🏮 签文视觉重做：中式签纸风

- **签头横幅**：顶部新增朱红渐变横幅「每 日 一 签」，日期/星期/星座/生肖移入横幅副标题（原独立日期行取消）。
- **签纸质感**：纸面加极淡横向纤维纹理（`repeating-linear-gradient`），四角加中式 L 形角饰；等级徽章从圆角胶囊改为**微倾斜的方形印章**（白边 + 外圈细线）。
- **签名**：29px 朱红渐变文字（`background-clip:text`），两侧金色装饰线，出现时弹入动画。
- **诗句**：左右金色竖线 + 淡纸底衬，右下角加旋转的「每日一签」朱红小印（模拟书画落款）。
- **解签**：左侧加「签」字朱红圆印；**宜忌**改为绿/红渐变小卡 + 圆角「宜」「忌」标签；**幸运三项**改为带「数/色/时」圆标的徽章。
- **出场动画**：签文整体淡入上浮 + 签名延迟弹入 + 诗句延迟上浮（`fRender` 每次重建 DOM，动画天然重放；`prefers-reduced-motion` 下由既有规则关闭）。
- **深色模式**：横幅/印章/渐变签名/诗句底/宜忌/徽章/按钮全套深色变体。
- 验证：DOM 桩 36 项全过（语法、CSS 括号配平、无重复 id、id 引用、新样式存在、fRender 结构/有生日与无生日渲染/复制字段）；真实浏览器截图核对浅色与深色效果。

---

## 2026-09-25 · 🎴 抽签前新增「摇签」动画（含看不到动画的修复）

- 点「🎴 今日一签」后不再直接出签文，先播一段**摇签动画**：木质签筒左右摇晃（0.44s 一个来回，带上下颠动），筒口露出 7 根竹签（红头、扇形散开、各自错峰上下跳动），摇完中间那根**升出筒口**，同时金色光晕扩散 + 四颗火星散开，然后才展开签文。
- 节奏：当天**第一次**抽摇 1.5s + 出签 0.8s；**回看已抽过的签**摇 0.43s + 0.64s（不拖沓）。**点动画任意位置可立即跳过**。
- 首次填生日后、以及「不填，直接抽」也都走同一段动画（原来是直接渲染签文）。
- 兼容性：系统开启「减弱动效」（`prefers-reduced-motion`）时**自动跳过动画直接出签**；深色模式下签筒配色同步调暗。
- 实现要点：新增 `fSkHtml/fStartShake/fSkipShake/fSkClear`，定时器统一登记，**关闭弹层或跳过都会清掉**，不会在后台误触发渲染；签支旋转角与延迟用 CSS 变量 `--r/--d` 传入（关键帧里靠 `rotate(var(--r))` 保留各自的倾角）；新增关键帧 `ftube/skbob/skout/skglow/skstar`（未复用已有的 `fshake`，避免与 🎴 图标动画冲突）。
- **修复"看不到动画"**：① 原来系统开启「减弱动效」时**整个跳过动画直接出签**，部分环境（Windows 关闭动画效果、部分无头浏览器）默认命中，导致完全看不到 —— 改为**降级版**：签筒静置、无左右摇晃，签支淡入抽出（`skfade`），保证有可见过渡又不做剧烈运动；② 回看已抽过的签原来只摇 0.43s，一闪而过 —— 加长到 0.8s。已用真实浏览器（可注入 `prefers-reduced-motion` 与放慢动画）逐帧采样验证：0.4–1s 筒身 ±8° 摇摆、签支错峰跳动，1.5s 停摇、签支升出。

## 2026-09-25 · 🔎 期刊浏览：期刊选择改为可输入搜索

- 「📚 期刊浏览」顶部的期刊下拉从原生 `<select>` 换成**可输入搜索的组合框**：直接打字即可过滤，支持**刊名 / 中文名 / 子学科 / 大类**四种匹配（搜 `development`、`金融`、`计量`、`JDE` 都行），命中处黄色高亮，结果仍按大类分组并标注该组本数。
- 交互：聚焦即展开，输入即过滤；**↑ ↓ 键**移动高亮、**回车**选中、**Esc**关闭、点空白处收起；**选中即自动加载该刊**（不用再点「查看」，按钮保留）。
- 细节：输入框显示「刊名（中文名）」，选中项高亮；无结果显示空态提示；原来空的 `#browseJournal` 改为 hidden input 存 key，`loadBrowse()` 读取方式不变；加了一道保护——未选刊时不再因 `j` 为 undefined 报错，而是提示「请先在上方选择一本期刊」。
- 深色模式与窄屏（≤560px 组合框占满一行）均已适配。

---

## 2026-09-25 · ❓ 新增「使用指引」：首次访问自动弹出，顶栏随时重开

- **新用户第一次打开会自动弹出一次**使用指引（`settings.guideSeen` 记住，默认已勾选「下次进入时不再自动弹出」）；顶栏新增 **「❓ 使用指引」** 按钮，随时可以重新打开；没看过指引时该按钮会带呼吸光圈提示。
- 指引分 6 章，点章节 chips 切换：**👋 这是什么**（定位、数据来自 OpenAlex、存本机不用登录、以及它不做什么）／**🚀 三步上手**（选期刊 → 看今日推荐 → 收藏记笔记；附引用篮、概念词典、AI Key 说明）／**🧭 各个页签**（十个页签逐个说明）／**🔖 写论文怎么用**（引用篮导出 GB-T 7714、综述助手、方法标签、相似文献、AI 读后三点）／**🐱 小彩蛋**（每日一签、小猫、留言墙、深色模式）／**❓ 常见问题**（论文来源、某刊为何为空、数据会不会丢、手机能用吗、AI 是否收费、合规性）。
- 关闭方式：右上角「关闭」/「开始使用 →」/ 点遮罩 / Esc；底部可勾选是否再自动弹出。
- `#guideModal` 位于主脚本之后，绑定沿用 `bindGuide()` 幂等 + `bootGuide()` 延迟到 DOMContentLoaded 的模式（同 `#askModal`）；启动链末尾追加 `bootGuide()`。

---

## 2026-09-25 · 💬 弹幕墙移出顶部，位置可切换

- **默认位置改为「右下角悬浮卡片」**：弹幕条不再横在页面顶部（原来占 98px 且滚动的弹幕一直干扰阅读）。新位置是右下角 326×176 的圆角卡片，带「💬 留言墙 · N 条」标题，点「⌄ 收起」可压成 50px 的一条。
- **位置可随时切换**：设置 → 💬 留言墙 → 「弹幕位置」三个按钮 —— **右下角悬浮（推荐）/ 页面底部 / 页面顶部（原位置）**，切换立即生效并记住（`settings.danmuPos`）。底部与顶部是文档流位置（会真的挪 DOM），右下角是 fixed。
- 轨道改用 `.l1/.l2/.l3` class 定位（原来是内联 `style="top:"`），位置切换才能覆盖；悬浮模式用专属动画 `dflyw`（横飞距离按卡片宽度 330px 计算，不再用 `100vw`，否则小窗里会空窗很久）。
- 窄屏（≤560px）悬浮模式自动降级为贴底横条，避免遮挡正文。

---

## 2026-09-25 · 🔻 今日推荐：删除「学科大类」，改「学科范围」（经济学 / 管理学）

- **删除「学科大类」行**：原先的来源多选（经济学 / 管理学 / 中国研究 / NBER）与下一行的子学科重复，已整行移除（含 `#dailyRegionSeg` 及其初始化、`getDailyRegions()` 降级为兼容函数恒返回全部）。
- **「子学科」改名为「学科范围」**，只保留**两行：经济学 / 管理学**。原 4 个来源按学科归入这两行：**经济学** = 国际经济学顶刊 + 工作论文 + 中国经济（5 本）= 89 本；**管理学** = 管理学期刊 + 中国管理（MOR）= 71 本。合计仍 160 本、27 个子学科，无遗漏。
- **功能不减**：「工作论文」「中国经济」「中国管理」仍作为**所在学科行内的可选项**保留（想只看 NBER/世行/IMF，点经济学行里的「工作论文」即可），且现在**可跨行任意组合**（如同时选「经济学·经济史」+「管理学·会计学」）。
- 子学科改用 **「来源|子学科」复合键** 标识，解决「综合 Top」在经济/管理学下同名的冲突；老用户存的设置自动迁移（`normDailySubs()`），无需重新选。

---

## 2026-09-25 · 🧱 分类区结构化重排（筛选面板 + 大类卡片 + 搜索）

- **今日推荐筛选区改为结构化面板**：原来的三条平铺横条（来源 / 子学科 / 方法）合并为一个带边框的 `.fpanel`，每行左侧固定宽度的**标签列**（学科大类 / 子学科 / 研究方法），右侧 chips 区，行间有分隔线，标签与内容左右对齐成"表格感"。
- **子学科按大类分行**：每个大类独占一行 `.fsubline`，行首是**该大类的配色标签**（经济学蓝 #185FA5 / 管理学绿 #0F6E56 / 中国经管棕 #8A5A2B / 工作论文灰 #5A6472），行尾显示「已选 2 / 14」；chips 选中时**用所属大类的颜色**填充，一眼可辨归属。
- **子学科可收起**：新增 ▴收起 / ▾展开 开关（状态记在 `settings.subsOpen`），收起时压缩为一行摘要「经济学：发展经济学、劳动与人口 ｜ 管理学：会计学」，默认展开。
- **设置页期刊区卡片化**：四大类各自成为一张 `.jcatcard` 卡片（左侧 4px 主题色条 + 大类名 + 「N 个子学科 · M 本 · 已选 X / Y」+ 全选/清空/加新刊），卡片头可点击**折叠整个大类**；子学科改为独立圆角块（浅底标题行 + 迷你进度条 + 缩进的期刊网格）。
- **新增工具条**：期刊名 / 中文名 / 子学科**搜索框**（命中处黄色高亮）、**全部 / 已选 / 新刊** 三档视图、**全部展开 / 全部收起**。
- **过滤安全性**：过滤只控制「子学科块显隐」，块内始终渲染该子学科的全部期刊（隐藏的仍在 DOM 中），保存设置前还会自动恢复全量渲染，**搜索状态下保存不会丢失未显示期刊的勾选**。

---

## 2026-09-25 · 🗂️ 期刊两级分类（学科大类 → 子学科）+ 期刊库扩至 160 本

- **新增两级分类**：`学科大类 → 子学科 → 期刊`。大类沿用原四类（经济学 / 管理学 / 中国经管研究 / 工作论文），下分 **27 个子学科**：经济学 14 个（综合 Top、发展、劳动与人口、公共与政策、宏观与货币、微观理论 IO、计量与统计、国际金融与贸易、金融学、环境与资源、卫生、行为与实验、经济史、区域与城市）、管理学 10 个（综合 Top、战略、组织行为与 HR、市场营销、运营与供应链、信息系统、会计、创业与创新、国际商务、商业伦理与公共管理）、中国经管 2 个、工作论文 1 个。
- **期刊库 87 → 160 本（+74）**：新增刊全部经 OpenAlex ISSN 接口核验（确保拉得到论文），例如 Journal of Urban Economics、Economic Development and Cultural Change、World Bank Economic Review、Labour Economics、Journal of Economic History、Leadership Quarterly、Human Resource Management、EJOR、Journal of Business Ethics、Public Administration Review 等；工作论文新增 **World Bank 政策研究工作论文** 与 **IMF 工作论文**。
- **今日推荐**：筛选条在「研究方法」之上新增 **子学科 chips 行**，随已选大类动态生成，可多选（不选 = 该大类全部），选择即时落盘；论文卡片上的分类标签由「国际经济学顶刊」改为**具体子学科名**（如「发展经济学」）。
- **设置页**：期刊勾选改为按大类分组、子学科可折叠（显示「已选 x / 共 y 本 · 新 N」），组内支持全选 / 清空 / 只加入本组新刊；顶部显示总数统计。
- **新刊不强制塞入**：新增刊默认 `def:false`，**绝不覆盖已有勾选**。设置页出现一次性横幅「本次新增 74 本已核验期刊」，可**一键加入全部** / **按子学科加入** / **暂不需要**。
- 期刊浏览下拉按大类分组（optgroup）；Review of Finance 从管理学移入金融学。

---

## 2026-09-25 · 🗂️ 设置页改为浏览器页签式分区

- 设置页从「一整页往下滚」改为 **6 个页签**，点开才显示对应模块：`📚 期刊与推荐` / `🤖 AI 接口` / `🎨 外观与目标` / `🎴 我的档案` / `💾 数据与同步` / `💬 留言墙`。
- 页签为浏览器标签风格（激活签浮起、与内容区连体），切换带淡入动画，深色模式同步适配。
- 记住上次停留的页签（`settings.setTab`），下次打开设置仍在该签。
- 卡片内容与按钮均未改动，只是分组收纳；原有设置项（期刊勾选、AI Key、IF 开关、生日、备份、Supabase、留言墙）功能与位置不变。

---

## 2026-09-24 · 📌 弹幕区可折叠 + 跨设备说明

- **折叠按钮**：弹幕条右侧新增「⌄ 收起 / ⌃ 展开」。收起后弹幕条压缩为一条 38px 细条，只保留「💬 留言墙」入口与「💬 弹幕已收起 · 共 N 条留言（点击展开）」提示；点击细条任意处或「⌃ 展开」即可恢复。收起后停止投放弹幕，不占版面也不耗资源。
- **状态记忆**：折叠/展开偏好保存在本机（localStorage），刷新或下次打开仍保持你的选择。
- **跨设备可见性说明**：状态提示文案已明确区分——未配置云端时为「本地预览模式」（留言只存在这台设备的浏览器里，换手机/换电脑看不到）；按设置页 3 步配置 Supabase 后即为「☁️ 全网公开」，任何设备的访客免登录可见、可留言。

## 2026-09-24 · 💬 弹幕留言墙（免登录评价区）

访客评价以**弹幕**形式在网页顶部滚动播放，无需登录即可发言，附「👍 点赞」与「✅ 已解决」互动按钮。

| 项目 | 说明 |
| --- | --- |
| 弹幕条 | 页面顶部新增 98px 弹幕轨道（3 条泳道轮流投放、约 2.7s 一条、匀速从右向左飘 17–28s），**鼠标悬停暂停**，点击任意弹幕打开留言墙面板；右侧常驻「💬 留言墙」按钮 |
| 留言墙面板 | 输入区（140 字上限 + 昵称可留空默认「匿名同学」 + 好评/建议/求助/报错标签）+ 完整列表（最新在前、「只看未解决」筛选） |
| 互动 | 👍 点赞（每人每条限一次，本地记录防重复刷）；✅ 标记已解决 / 撤销，已解决条目置灰斜纹 |
| 免登录 | 发布与互动均匿名进行，无需注册登录；20 秒防刷间隔；正文 XSS 转义 |
| 数据层 | **双层降级**：已配置 Supabase（设置页「云端同步」的 URL + anon key，无需登录）→ 全网公开共享（表 `econdaily_wall`，点赞/解决走 `wall_like` / `wall_resolve` RPC）；未配置 → 本地预览模式（仅自己可见，localStorage 落盘） |
| 配置 | 设置页新增「💬 留言墙」卡片：状态显示、弹幕开关、打开面板、**一键复制建表 SQL**（含 RLS 与 RPC 授权，3 步约 3 分钟接入） |
| 示例 | 无任何留言时展示 4 条示例（明确标注），发布第一条真实留言后自动消失 |
| 实测 | 真浏览器：弹幕自动滚动（3 泳道分布正常）、发布→点赞+1→标记已解决→新留言即时进入弹幕、本地落盘，全部通过 |

---

## 2026-09-24 · 🎴 每日一签（个性化抽签）

科研疲惫时的调剂小功能：**不点不出现，点了才抽签**。

| 项目 | 说明 |
| --- | --- |
| 入口 | 左下角伴读小猫上方新增「🎴 今日一签」按钮；设置页「我的小档案」也可直接抽 |
| 触发方式 | 仅用户点击才出现，绝不自动弹窗；当天抽过后按钮变「🎴 今日已抽」，可重复查看 |
| 每日换新 | 以「当天日期 + 生日」为种子做确定性抽签：同一天永远是同一支签，跨天自动换新签 |
| 好签居多 | 五档全为吉/平签，**无凶签**。上上签 12% / 上签 26% / 中吉 30% / 小吉 22% / 平安签 10%（上上+上+中吉 ≈ 七成） |
| 签文内容 | 40 支签，每支含：四字签名 + 两句签诗 + 白话解签（科研场景：投稿/写作/数据/心态）+ 宜 / 忌 + 幸运数字 / 幸运色 / 幸运时段 + 猫猫寄语 |
| 个性化 | 按出生日期生成星座、生肖、生命灵数（幸运数字基数）、季节；生日当天强制「生辰上上」特签，生辰月与生日前 7 天有专属文案 |
| 数据 | 生日存 `localStorage['econdaily_birthday']`，**仅本机浏览器，不上传不同步**；抽签状态存 `econdaily_fortune`（含连抽天数、累计签数、近 60 天签谱） |
| 入口位置 | 设置 → 🎂 我的小档案（保存 / 清除 / 立刻抽一签）；未填生日时首次点击会先弹出填写页，也可选「不填，直接抽」 |

---

## 2026-09-24 · 论文卡片显示影响因子（IF）

| 项目 | 说明 |
| --- | --- |
| 覆盖范围 | 所有走论文卡片的推荐位：今日推荐、期刊浏览、研究助手、经典必读、相似文献、作者追踪、文件夹、综述检索（中英文一视同仁） |
| 数据来源 | OpenAlex `sources.summary_stats['2yr_mean_citedness']`——该刊近两年篇均被引，口径与 JCR 影响因子相同（两年 / 篇均），收录范围更广 |
| 显示形式 | 卡片 meta 区蓝色胶囊 `IF 15.5`；悬停显示「影响因子 + H 指数 + 刊名」 |
| 性能 | 按期刊维度批量请求（每批最多 50 个），结果写入 `localStorage`（`econdaily_ifcache_v1`），同一期刊只请求一次 |
| 渲染方式 | DOM 补丁更新（`paintIF`），不整列表重渲染，弹层/展开状态不丢失 |
| 例外 | 工作论文、预印本、书籍等非 journal 类型不显示；无指标的期刊显示灰色「IF 暂无」标签（多为中文期刊，CNKI 未接入），悬停可看说明 |
| 实测 | 真实浏览器验证：今日推荐 20/20 张卡片均拿到 IF（如 JPE 6.2、ISR 5.9、SMJ 8.6、POM 4.8）；中文刊《管理世界》显示「IF 暂无」并给出查阅建议 |
| 开关 | 设置 → 「论文卡片显示影响因子（IF）」复选框，默认开启，切换即时生效（CSS `body.no-if`） |

**已知局限（已写入设置页提示）**：OpenAlex 不收录 CNKI，中文期刊的被引统计不完整，数值通常明显低于其在中文数据库中的实际影响力，仅供参考。

---

## 2026-09-10 · 学术图标改版

| 区域 | 改动 |
| --- | --- |
| Favicon | 新设计：深蓝对角渐变圆角底 + 打开的书（白页+深蓝书脊+6 行文本） + 学术金上升折线 + 终点金点 |
| 头部小 logo（`.mark`） | 与 Favicon 同款，19×19 省略文本行，stroke 加粗以保证小尺寸可读 |
| 配色 | 深蓝 `#2b4d7a → #0f2444` + 学术金 `#f5b840` + 纯白书页 |
| 影响范围 | 仅视觉，功能零变更；四份副本（沙箱 / GitHub Pages / 本地版） md5 一致 |

---

## 2026-09-10 · 三项交互增强

### 1. 期刊浏览 · 关键词搜索
- 工具条新增输入框 `🔍 在已加载论文中搜索标题/摘要/作者…`
- 在已加载结果（OpenAlex 接口缓存）中按关键词本地过滤，**不重新请求接口、不消耗 API 额度**
- 实时显示「已加载 N 篇 / 在已加载的 N 篇中匹配到 M 篇」；无结果时引导「加载更多」扩大范围
- 原有「选期刊 → 加载 → 加载更多」逻辑完全保留

### 2. 我的笔记 · 导出 TXT
- 导出菜单从两项（Word / PDF）扩展为三项（Word / TXT / PDF）
- `noteToText(html)`：HTML → 纯文本，h1-h6 成行、`<br>`/块级换行、`<li>` 转 `- `、待办转 `☐`/`☑`
- 输出 `.txt` 带 **UTF-8 BOM**，Windows 记事本打开不乱码

### 3. 今日推荐 · 来源 / 方法 双多选
- **来源**：原下拉框改为 4 个可多选 chip（🌐国际经济学 / 📊管理学 / 🇨🇳中国研究 / 📝NBER），可任意取消
  - 互斥保证：每个期刊只归属一个 region（intl/mgmt/cn/wp），多选天然互斥、不重不漏
  - 全取消会被拦下（至少保留一个）
- **方法**：原下拉框改为 17 个标签 chip +「清空」按钮，可点选多个
  - 17 类：因果推断、双重差分(DID)、工具变量(IV)、断点回归(RDD)、随机实验、合成控制、面板数据、事件研究、文本分析、机器学习、计量经济学、结构模型、贝叶斯、元分析、匹配、问卷/调查、网络分析
  - 过滤逻辑：**并集（命中任一即可）**——因方法标签为一篇可多标签，无法做到严格互斥
- 提示语动态显示「已选 N 项（命中任一即可）/ 未选=全部」

---

## 2026-09-08 · 学生向·小改陆续

- 「伴读小猫咪」交互稳定化：去掉 `setPointerCapture` / `preventDefault` 误吞 click，改 `pointerup` 位移阈值（>5px）区分单击 + 400ms 去重闸门
- 「我的笔记」模块：富文本 + 模板 + 本地 / Supabase 同步
- 笔记导出：Word（HTML→.doc）/ PDF（window.print）/ TXT（本次新增）
- 笔记文件夹：新建 / 筛选 / 拖拽归类（注意：`btnNewNoteFolder` 与历史 `btnNewFolder` 同 id 已修复）
- 原生 `prompt` / `confirm` / `alert` → 自建 `#askModal`（iframe 沙箱预览中原生会被静默拦截）
- 设置页 AI 服务商扩展至 11 家 + 建议区
- 重复 id 扫描列入必做验证

---

## 2026-09-09 · GitHub Pages 正式发布

- 仓库 `AnnieRosin/econdaily`（Public，main 分支）
- 站点：**https://annierosin.github.io/econdaily/**（给他人长期分享的唯一正式网址）
- 发布走 GitHub Contents API（`git push` 在本机静默失败）
- 已加 `<meta name="description">` + `og:title/description/type`，便于转发时显示简介卡片
- 已做安全扫描：本项目无硬编码密钥 / 邮箱 / JWT，可安全公开

---

## 已具备的核心功能（截至 2026-09-10）

| 模块 | 内容 |
| --- | --- |
| 今日推荐 | 严格时间倒序 + 加载更多 + 4 来源 / 17 方法多选 + 关键词筛选 |
| 期刊浏览 | 单刊历年论文 + 关键词本地搜索 |
| 研究助手 | 全文检索 + 相关度 / 时间排序 + 中文通道 + 方法筛选 |
| 经典必读 | 经济学 / 管理学两大类，CLASSIC_VER 缓存版本号 |
| 数据库 + 实证方法 | 资源导航 |
| 作者 / 团队追踪 | 关注多位学者，自动聚合最新论文 |
| 个人阅读统计 | SVG 可视化 |
| 引用篮 | 勾选多篇 → 自动编号参考文献列表（BibTeX / GB-T7714） |
| 概念词典 | 摘要 / 速读术语可点弹解释（GLOSSARY+TERM_ALIAS） |
| 相关文献推荐 | 📎 相似（OpenAlex `related_works`） |
| AI 读后三点 | 核心结论 / 贡献 / 局限 / 方法与数据 |
| 我的笔记 | 富文本 + 模板 + Word/PDF/TXT 导出 + 文件夹 + Supabase 云同步 |
| 数据备份 | 导出 / 导入全部 localStorage；笔记 + 文件夹导出 Markdown / Word |
| 全局搜索 | 跨所有数据 |
| 伴读小猫咪 | 桌面拖动 + 俏皮语录 |
| 暗色主题 | 跟随系统 |
| 11 家 AI 服务商 | 设置页可配置 |

---

## 部署渠道

| 渠道 | 地址 | 用途 |
| --- | --- | --- |
| GitHub Pages | https://annierosin.github.io/econdaily/ | 给他人长期分享 |
| CloudStudio 沙箱 | 平台按工作区复用 sandboxId `9009fb9a1c184a009f9203ea6a9d2927` | 临时演示（沙箱会回收） |
| 本地离线版 | `EconDaily本地版/EconDaily.html` | 双击即用 |
